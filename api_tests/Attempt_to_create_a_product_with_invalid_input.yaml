# Test generated by RoostGPT for test intergration-test-artillery-gherkin-demo using AI Type Open AI and AI Model gpt-4


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "Attempt_to_create_a_product_with_invalid_input.csv"
    fields:
      - "description"
      - "price"
    skipHeader: true
  defaults:
    headers:
      Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Attempt to create a product with invalid input"
    flow:
      - log: "Given the product creation API is available"
      - post:
          url: "/products"
          json:
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 405
            - hasProperty: "error"
            - equals:
                - "error"
                - "Invalid input"
          think: 1
      - log: "Then the response code should be 405"
      - log: "And the response should indicate an 'Invalid input' error"

# Test generated by RoostGPT for test intergration-test-artillery-gherkin-demo using AI Type Open AI and AI Model gpt-4


config:
  target: "{{ $processEnvironment.API_HOST }}/products"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "Attempt_to_retrieve_a_product_with_a_non_existing_ID.csv"
    fields:
      - "productId"
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}/products"
      http:
        timeout: 10

scenarios:
  - name: "Attempt to retrieve a product with a non-existing ID"
    flow:
      - log: "Given the products API is available"
      - get:
          url: "/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: 'message'
            - equals: 
                - "message"
                - "Product not found"
          afterResponse: "logResponse"
      - think: 1

processors:
  logResponse:
    - function(request, response){
        console.log('Response for request', request.url, 'was', response.statusCode);
      }

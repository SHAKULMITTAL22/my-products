# ********RoostGPT********

# Test generated by RoostGPT for test intergration-test-artillery-gherkin-demo using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# ROOST_METHOD_HASH=803fe763f5
# ROOST_METHOD_SIG_HASH=c56a467b87
# 
#    ########## Scenario ##########  
# 
# {
#   background: 'Given the base URL is "http://localhost:8080"',
#   rule: null,
#   scenario: 'Grant authorization\r\n' +
#     '    Given an existing account, create another account\r\n' +
#     '    When the client sends a POST request to "/authorizations"\r\n' +
#     '    Then grant access to your account and confirm access via GET request to "/authorizations"',
#   title: 'Grant authorization'
# }
# 

# ********RoostGPT********
config:
  target: "http://localhost:8080"
  phases:
    - duration: 10
      arrivalRate: 5
      name: Warm up
  variables:
    accountId: "123456"
    newAccountId: "789012"

scenarios:
  - name: "Grant authorization"
    flow:
      - post:
          url: "/authorizations"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            accountId: "{{ newAccountId }}"
          capture:
            json: "$.authorizationId"
            as: "authId"
          expect:
            - statusCode: 201
            - hasProperty: authorizationId
            - contentType: application/json
          log: "Expecting a 201 response with authorizationId for successful authorization grant"
      
      - think: 2

      - get:
          url: "/authorizations/{{ authId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: authorizationId
            - equals:
                - "{{ authId }}"
                - "$.authorizationId"
            - hasProperty: accountId    
            - equals: 
                - "{{ newAccountId }}"
                - "$.accountId"
            - hasProperty: createdAt
            - hasProperty: updatedAt
            - contentType: application/json
          log: "Expecting a 200 response with authorization details matching the created one"

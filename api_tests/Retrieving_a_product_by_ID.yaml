# Test generated by RoostGPT for test intergration-test-artillery-gherkin-demo using AI Type Open AI and AI Model gpt-4


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "Retrieving_a_product_by_ID.csv"
    fields:
      - "productId"
    skipHeader: true

scenarios:
  - name: "Retrieving a product by ID"
    flow:
      - get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "productId"
            - equals:
                - "{{ productId }}"
                - "{{ response.body.productId }}"
          think: 1
